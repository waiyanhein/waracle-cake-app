# This file would not be commited in the real-world
# In the real-world application, this might be the CDN domain.
ASSET_DOMAIN="http://localhost:3000"
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/cake_db"
STORAGE_DIRECTORY="prod_storage"
NODE_ENV="production"
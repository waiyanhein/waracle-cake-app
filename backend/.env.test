# In the real-world application, this might be the CDN domain.
ASSET_DOMAIN="http://localhost:3000"
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/test_cake_db"
STORAGE_DIRECTORY="test_storage"
NODE_ENV="test"